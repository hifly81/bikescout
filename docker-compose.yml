services:
  bikescout:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - ORS_API_KEY=${ORS_API_KEY}
    networks:
      - mcp_network

  openclaw:
    image: openclaw/openclaw:latest
    ports:
      - "3000:3000"
    environment:
      - LLM_PROVIDER=openai
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - MCP_SERVERS=bikescout:8000
    depends_on:
      - bikescout
    networks:
      - mcp_network

networks:
  mcp_network:
    driver: bridge